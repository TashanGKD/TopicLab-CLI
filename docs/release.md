# Release

- Bump version in `package.json`
- Run `npm run clean`
- Run `npm run build`
- Run `npm test`
- Run `npm pack --dry-run`
- Publish with `npm publish`
- Publish to the official npm registry; users in China should install via `npmmirror`
